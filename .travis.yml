language: python
python:
  - "3.2"
  - "3.3"
  - "3.4"
install:
  - "pip install ."
before_script:
  - "python -m nala.download_corpora"
  - "curl -L https://github.com/downloads/chokkan/crfsuite/crfsuite-0.12-x86_64.tar.gz -o crfsuite.tar.gz"
  - "tar -xvf crfsuite.tar.gz"
script:
  - "nosetests"
  - "python demo_predict.py -c ./crfsuite-0.12/bin/ -d example.txt"
